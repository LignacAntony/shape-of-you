{% extends 'base.html.twig' %}

{% block title %}OutfitItem{% endblock %}

{% block body %}
    <h1>OutfitItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ outfit_item.id }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ outfit_item.size }}</td>
            </tr>
            <tr>
                <th>PurchaseAt</th>
                <td>{{ outfit_item.purchaseAt ? outfit_item.purchaseAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_outfit_item_index') }}">back to list</a>

    <a href="{{ path('app_outfit_item_edit', {'id': outfit_item.id}) }}">edit</a>

    {{ include('outfit_item/_delete_form.html.twig') }}
{% endblock %}
