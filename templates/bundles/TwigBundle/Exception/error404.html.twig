{% extends 'base.html.twig' %}

{% block title %}Page non trouvée - Shape Of You{% endblock %}

{% block body %}
<div class="min-h-[80vh] flex items-center justify-center px-4 sm:px-6 lg:px-8">
    <div class="max-w-max">
        <main class="sm:flex">
            <p class="text-4xl font-extrabold text-secondary-400 dark:text-secondary-500 sm:text-5xl">404</p>
            <div class="sm:ml-6">
                <div class="sm:border-l sm:border-gray-200 dark:border-gray-700 sm:pl-6">
                    <h1 class="text-4xl font-extrabold text-gray-900 tracking-tight sm:text-5xl">
                        Page non trouvée
                    </h1>
                    <p class="mt-4 text-base text-gray-500 dark:text-gray-400">
                        Désolé, nous n'avons pas trouvé la page que vous recherchez.
                    </p>
                    <p class="mt-2 text-base text-gray-500 dark:text-gray-400">
                        Il est possible que la page ait été déplacée ou supprimée.
                    </p>
                </div>
                <div class="mt-10 flex space-x-3 sm:border-l sm:border-transparent sm:pl-6">
                    <a href="{{ path('app_home') }}" 
                       class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-secondary-400 hover:bg-secondary-500 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-secondary-500 transition-colors duration-200">
                        Retour à l'accueil
                    </a>
                    
                </div>
            </div>
        </main>

        {% if app.debug %}
            <div class="mt-8 bg-gray-50 dark:bg-gray-800 rounded-lg p-4">
                <h2 class="text-lg font-semibold text-gray-900 dark:text-white mb-2">
                    Informations de débogage
                </h2>
                <div class="text-sm text-gray-600 dark:text-gray-300">
                    <p>URL demandée : {{ app.request.uri }}</p>
                    {% if exception %}
                        <p class="mt-2">Message d'erreur : {{ exception.message }}</p>
                    {% endif %}
                </div>
            </div>
        {% endif %}
    </div>
</div>
{% endblock %} 