{% extends 'base.html.twig' %}

{% block title %}Accès refusé - Shape Of You{% endblock %}

{% block body %}
<div class="min-h-[80vh] flex items-center justify-center px-4 sm:px-6 lg:px-8">
    <div class="max-w-max">
        <main class="sm:flex">
            <p class="text-4xl font-extrabold text-secondary-400 dark:text-secondary-500 sm:text-5xl">403</p>
            <div class="sm:ml-6">
                <div class="sm:border-l sm:border-gray-200 dark:border-gray-700 sm:pl-6">
                    <h1 class="text-4xl font-extrabold text-gray-900  sm:text-5xl">
                        Accès refusé
                    </h1>
                    
                    <p class="mt-4 text-base text-gray-500 ">
                        Désolé, vous n'avez pas les permissions nécessaires pour accéder à cette page.
                    </p>
                </div>
                <div class="mt-10 flex space-x-3 sm:border-l sm:border-transparent sm:pl-6">
                    <a href="{{ path('app_home') }}" 
                       class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-secondary-400 hover:bg-secondary-500 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-secondary-500 transition-colors duration-200">
                        Retour à l'accueil
                    </a>
                    {% if not app.user %}
                        <a href="{{ path('app_login') }}" 
                           class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-secondary-700 dark:text-secondary-300 bg-secondary-100 dark:bg-secondary-800 hover:bg-secondary-200 dark:hover:bg-secondary-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-secondary-500 transition-colors duration-200">
                            Se connecter
                        </a>
                    {% endif %}
                </div>
            </div>
        </main>
    </div>
</div>
{% endblock %} 