{% extends 'base.html.twig' %}

{% block title %}New ClothingItem{% endblock %}

{% block body %}
    <h1 class="text-2xl font-bold mb-6">Create New ClothingItem</h1>

    <!-- Container for the form -->
    <div class="max-w-md mx-auto bg-white p-6 rounded-md shadow-md">
        {{ include('admin/clothing_item/_form.html.twig', {'button_label': 'Create'}) }}
    </div>

    <!-- Back to list link -->
    <div class="mt-4">
        <a
                href="{{ path('app_clothing_item_index') }}"
                class="inline-block px-4 py-2 bg-blue-500 text-white font-semibold rounded hover:bg-blue-600 transition-colors"
        >
            Back to list
        </a>
    </div>
{% endblock %}
