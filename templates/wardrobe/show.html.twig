{% extends 'base.html.twig' %}

{% block title %}Wardrobe{% endblock %}

{% block body %}
    <h1>Wardrobe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ wardrobe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ wardrobe.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ wardrobe.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ wardrobe.createdAt ? wardrobe.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_wardrobe_index') }}">back to list</a>

    <a href="{{ path('app_wardrobe_edit', {'id': wardrobe.id}) }}">edit</a>

    {{ include('wardrobe/_delete_form.html.twig') }}
{% endblock %}
